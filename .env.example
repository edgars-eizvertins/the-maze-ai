# Copy to .env and fill in. NEVER commit the real .env.
# Generate a strong key, e.g.:  openssl rand -base64 48
JWT_KEY=replace_me_with_a_long_random_secret_at_least_32_chars

# Host port the game is served on (http://<host>:<UI_PORT>)
UI_PORT=8090
